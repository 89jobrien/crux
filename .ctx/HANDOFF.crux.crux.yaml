project: crux
id: crux
updated: "20260417:220000"
description: >
  An agentic DSL for Rust — inspectable, serializable, replayable agent execution.
  Rust-embedded DSL (macros, traits, types) for writing agentic programs where every
  step, branch, delegation, and failure is a first-class value.

items:
  - id: crux-20
    name: baml-llm-extract
    title: "Wire BAML structured output into llm::extract handler"
    description: >
      Add optional baml feature flag to crux-agentic. Implement llm::extract handler
      backed by BAML for schema-validated structured LLM output (ExtractEntities,
      Summarize, Classify). llm::complete stays unchanged. Integration tests cfg-gated
      on OPENAI_API_KEY / ANTHROPIC_API_KEY.
    priority: P1
    status: closed
    files:
      - crates/crux-agentic/Cargo.toml
      - crates/crux-agentic/baml_src/
      - crates/crux-agentic/src/llm.rs
      - crates/crux-agentic/src/lib.rs
      - crates/crux-agentic/tests/llm_extract.rs
    extra:
      - type: note
        note: >
          Implementation committed d9cbaa0 (branch: main). End-to-end validated session 13
          with real OPENAI_API_KEY: all 33 tests pass (classify 2.7s, summarize 3.3s,
          extract_entities 4.4s). No separate commit for the live run; d9cbaa0 is the
          authoritative implementation reference.

  - id: crux-21
    name: demo-pipeline
    title: "End-to-end demo pipeline: file → BAML extract → structured output"
    description: >
      Write YAML pipelines exercising the full crux-agentic stack. At minimum:
      examples/extract_summary.yaml (fs::read → llm::extract Summarize → json::write)
      and examples/extract_entities.yaml. Update README with run instructions,
      sample output, and API key note.
    priority: P1
    status: closed
    files:
      - examples/extract_summary.yaml
      - examples/extract_entities.yaml
      - README.md
    extra:
      - type: note
        note: >
          Committed df3ae65 (branch: main). Both example YAML pipelines completed with
          input fixtures. README updated with pipeline examples section and API key
          setup instructions.

  - id: crux-22
    name: publish-crux-agentic
    title: "Publish crux-agentic v0.2.2 to crates.io"
    description: >
      Audit Cargo.toml metadata, add lib-level rustdoc, ensure baml feature is
      optional and documented, cargo publish --dry-run, then publish.
    priority: P2
    status: closed
    files:
      - crates/crux-agentic/Cargo.toml
      - crates/crux-agentic/src/lib.rs
    extra:
      - type: note
        note: >
          Version bumped 0.2.1 → 0.2.2 in commit dca076f (branch: main) to include baml
          feature. Published to crates.io session 13. Live at
          https://crates.io/crates/crux-agentic/0.2.2

  - id: crux-23
    name: planner-design
    title: "Design crux-planner: goal-to-pipeline generation"
    description: >
      Write docs/planner-design.md covering: new crate vs YAML spec + BAML function,
      input schema (NL goal vs structured intent), static vs dynamic pipeline output,
      interaction with crux-script ArmDef/StepNode schema. Design only, no implementation.
    priority: P2
    status: closed
    files:
      - docs/planner-design.md
    extra:
      - type: note
        note: >
          Committed 962c377 (branch: main). Comprehensive design doc covering both
          BAML-based and Rust-crate paths, input/output schemas, composition rules,
          integration with crux-script, and 4-phase implementation roadmap.

log:
  - date: "20260417:220000"
    session: 13
    summary: >
      Closed crux-20: validated llm::extract end-to-end with real OPENAI_API_KEY (33/33
      tests pass). Closed crux-22: bumped crux-agentic to v0.2.2 and published to
      crates.io with baml feature included. Restored description fields and ISO-8601
      timestamps per council strict-critic findings.
    commits:
      - sha: d9cbaa0
        branch: main
        note: "crux-20 implementation (llm::extract + BAML schemas)"
      - sha: dca076f
        branch: main
        note: "crux-22 version bump 0.2.1→0.2.2"
      - sha: c003ff2
        branch: main
        note: "session 13 handoff update"
  - date: "20260417:204500"
    session: 12
    summary: >
      Completed crux-21 (demo pipeline examples + README). Committed both
      extract_summary.yaml and extract_entities.yaml with input fixtures. Updated README
      with pipeline examples section and API key setup instructions. Completed crux-23
      (planner design): comprehensive spec covering BAML-based and Rust-crate paths,
      input/output schemas, composition rules engine, integration with crux-script
      ArmDef/StepNode, and 4-phase implementation roadmap. crux-22 moved to
      pending-validation (awaits crux-20 API key validation).
    commits:
      - sha: df3ae65
        branch: main
      - sha: 962c377
        branch: main
  - date: "20260417:185600"
    session: 11
    summary: >
      Added llm::extract handler to crux-agentic behind optional baml feature flag.
      BAML schemas (ExtractEntities, Summarize, Classify) + generated client checked in.
      4 integration tests (3 API-gated). Linear issues JOB-246 through JOB-249 created;
      mirrored to GitHub as crux#1-4. Demo pipeline (crux-21) forge agent interrupted
      before commit.
    commits:
      - sha: d9cbaa0
        branch: main
  - date: "20260417:142700"
    session: 10
    summary: >
      Crux-agentic crate completed with full handler suite (ctrl, shell, fs, git, json,
      llm modules, 17 handlers total), smoke tests, register_all inventory. ArmDef schema
      upgraded: join_all and pipe stages, speculate arms accept full step objects with
      embedded args. Static args injection on StepNode and RouteBranch. crux-run binary
      migrated from crux-script to crux-agentic (avoids circular dep). joe/ YAML examples
      rewritten to canonical handler names. crux-19 closed: slashcrux dep wired,
      Priority/Urgency/ExecutionContext/StepState re-exported from prelude. Workspace
      bumped to v0.2.1, serde_yaml swapped for serde-saphyr. All 226 tests passing,
      cargo check clean.
    commits:
      - sha: 0fc2808
        branch: main
  - date: "20260417:080000"
    session: 9
    summary: >
      Published slashcrux v0.1.0 then v0.1.1 (renamed StepStatus -> StepState,
      lifecycle vs outcome distinction). Wired cruxai-core to depend on slashcrux v0.1.1;
      re-exported Priority, Urgency, ExecutionContext, StepState from cruxai prelude.
      Bumped workspace to v0.2.1, published all 5 cruxai crates. crux-19 closed.
    commits:
      - sha: 778389f
        branch: main
      - sha: 8d37ce3
        branch: main
  - date: "20260416:000000"
    session: 8
    summary: >
      Architecture design — introduced slashcrux shared vocabulary crate
      (Priority, Urgency, StepStatus, ExecutionContext). crux-core Cargo.toml wired to
      depend on slashcrux. crux-19 opened to complete StepStatus migration in types/step.rs.
  - date: "20260413:000000"
    session: 7
    summary: >
      Completed crux-17 publish-readiness (README, CHANGELOG, deny.toml, examples).
      Added crux-script crate and 7 YAML pipeline examples. Renamed all four crates to
      cruxai-* (crux-18). Published cruxai-core, cruxai-macros, cruxai-script, cruxai
      v0.1.0 to crates.io. 196/196 tests passing.
    commits:
      - sha: 1dd57d1
        branch: main
      - sha: c50a74a
        branch: main
      - sha: c888375
        branch: main
      - sha: 21914aa
        branch: main
