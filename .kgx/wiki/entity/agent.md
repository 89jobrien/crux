---
title: Agent
tags: [trait, core, port]
---
# Agent
**Crate:** [[crux-runtime]] | **File:** `crates/crux-runtime/src/agent.rs`

Port trait for agentic work units.

## Associated Types
- `Input`: Serialize + DeserializeOwned + Send
- `Output`: Serialize + DeserializeOwned + Send

## Required Methods
- `name()` → `&'static str`
- `run(ctx: &mut CruxCtx, input: Self::Input)` → Future<Result<Output, CruxErr>>

## Default Methods
- `budget()` → [[Budget]] (default: unlimited)
- `on_low_confidence(score: f32)` → [[Recovery<T>]] (default: Continue)
- `on_step_failure(err)` → [[Recovery<T>]] (default: Propagate)

## Generated By
[[#[crux::agent]]] proc macro generates Agent impl from async fn.
