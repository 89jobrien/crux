{
  "text": "# crux-agentic Implementation Plan\n\n**Goal:** Add a `crux-agentic` crate that provides built-in, locally-executable step handlers (shell, fs, git, json, ctrl, llm) which register into `HandlerRegistry` so YAML pipelines are runnable without custom Rust code.\n\n**Architecture:** `crux-agentic` is a pure handler library — no new types, no new traits. Each module exposes a `register(registry: &mut HandlerRegistry)` function that installs its handlers under namespaced keys (e.g. `shell::capture`, `git::staged_files`). A top-level `crux_agentic::register_all(registry)` call wires everything.\n\n**Tech Stack:** Rust 2024, `reqwest`, `serde_json`, `tokio::process`, `cruxai-script`, `cruxai-core`.\n\n## Task 1: Workspace setup\n\nAdd `crux-agentic` crate skeleton and reqwest dependency to workspace Cargo.toml. Create lib.rs with `register_all()` and error.rs with `AgenticError`.\n\nFiles: `crates/crux-agentic/Cargo.toml`, `crates/crux-agentic/src/lib.rs`, `crates/crux-agentic/src/error.rs`, `Cargo.toml` (workspace root)\n\n## Task 2: Shell handlers\n\nImplement `shell::exec` (fire-and-forget) and `shell::capture` (returns stdout/stderr/exit_code). Both use `tokio::process::Command`. Fail on non-zero exit unless `args.allow_failure` is set.\n\nFiles: `crates/crux-agentic/src/shell.rs`, `crates/crux-agentic/tests/shell.rs`\n\n## Task 3: Filesystem handlers\n\nImplement `fs::read` (read file to string), `fs::write` (write string to file), `fs::glob` (expand glob pattern, return path list), `fs::exists` (boolean existence check).\n\nFiles: `crates/crux-agentic/src/fs.rs`, `crates/crux-agentic/tests/fs.rs`\n\n## Task 4: Git handlers\n\nImplement `git::staged_files`, `git::diff`, `git::log`, `git::status` using `shell::capture` internally. Parse output into structured JSON.\n\nFiles: `crates/crux-agentic/src/git.rs`, `crates/crux-agentic/tests/git.rs`\n\n## Task 5: JSON handlers\n\nImplement `json::pick` (extract keys), `json::merge` (merge two objects), `json::jq` (apply jq expression via subprocess).\n\nFiles: `crates/crux-agentic/src/json.rs`, `crates/crux-agentic/tests/json_handlers.rs`\n\n## Task 6: Control flow handlers\n\nImplement `ctrl::log` (log to stderr, pass through), `ctrl::noop` (identity), `ctrl::assert` (fail pipeline if condition false).\n\nFiles: `crates/crux-agentic/src/ctrl.rs`, `crates/crux-agentic/tests/ctrl.rs`\n\n## Task 7: LLM handler\n\nImplement `llm::complete` with OpenAI-compat and Anthropic paths via raw reqwest HTTP. No AI SDK dependency. Select provider via `args.provider` field.\n\nFiles: `crates/crux-agentic/src/llm.rs`, `crates/crux-agentic/tests/llm.rs`\n\n## Task 8: crux-run binary\n\nAdd `[[bin]] name = \"crux-run\"` to crux-agentic Cargo.toml. Implement `src/bin/run.rs`: parse pipeline YAML, load input JSON, build HandlerRegistry via `register_all`, run pipeline, print trace + output.\n\nFiles: `crates/crux-agentic/src/bin/run.rs`, `crates/crux-agentic/Cargo.toml`\n\n## Task 9: Pipeline examples\n\nAdd `examples/extract_entities.yaml`, `examples/extract_summary.yaml`, and `examples/decompose_spec.yaml` with matching input fixtures. Document `crux-run` usage in README.\n\nFiles: `examples/extract_entities.yaml`, `examples/extract_summary.yaml`, `examples/decompose_spec.yaml`, `examples/input_*.json`, `README.md`"
}
