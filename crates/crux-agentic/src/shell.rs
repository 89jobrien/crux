/// Shell handlers — re-exported from `crux-stdlib`.
///
/// This module delegates to `crux_stdlib::shell` for backwards compatibility.
/// New code should depend on `crux-stdlib` directly.
pub use crux_stdlib::shell::register;
