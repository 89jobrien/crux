//! Path B: user-composable, deterministic rule-based pipeline composer.
//!
//! [`PlanRule`] and [`RulePlanner`] are re-exported from `crux-types` — the
//! canonical definition shared with `crux-agentic::bin::crux`.

pub use crux_types::planner::{PlanRule, RulePlanner};
